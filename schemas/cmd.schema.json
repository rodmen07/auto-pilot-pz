{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AutoPilot Command",
  "description": "Legacy command schema; retained for tooling compatibility. The mod is fully autonomous — no external command file is required or read at runtime.",
  "type": "object",
  "required": ["action", "reason"],
  "additionalProperties": false,
  "properties": {
    "action": {
      "type": "string",
      "enum": ["eat", "drink", "sleep", "rest", "exercise", "outside", "fight", "flee", "bandage", "idle"]
    },
    "reason": {
      "type": "string",
      "minLength": 1
    }
  }
}
